{
  "deploy": {
    "startCommand": "php -S 0.0.0.0:$PORT -t public",
    "preDeployCommand": "php artisan config:clear && php artisan route:clear && php artisan view:clear && php artisan optimize:clear"
  }
}
